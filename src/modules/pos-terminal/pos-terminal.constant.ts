export enum PosTerminalSummaryStatus {
  PENDING = "pending",
  COMPLETED = "completed",
  REFUNDED = "refunded",
  CANCELLED = "cancelled",
}

export enum PosTerminalStatus {
  ACTIVE = "active",
  INACTIVE = "inactive",
  SUSPENDED = "suspended",
  TERMINATED = "terminated",
}
